--- frontend/src/app/form/[shareToken]/page.tsx
+++ frontend/src/app/form/[shareToken]/page.tsx
@@ -17,7 +17,8 @@
 // ─────────────────────────────────────────────────────────────────────────────
 // Welcome screen (sc11)
 // ─────────────────────────────────────────────────────────────────────────────
-function WelcomeScreen({ form, onStart }: { form: FormResponse; onStart: () => void }) {
+function WelcomeScreen({ form, onStart }: { form: FormResponse; onStart: () => void }) {
+  const btnColor = form.design_settings?.buttonColor || '#2563eb';
   return (
     <div className="flex-1 flex flex-col items-center justify-center px-8 text-center">
       <motion.div
@@ -28,7 +29,8 @@
         <button
           onClick={onStart}
           className="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 px-8 rounded text-lg transition-colors shadow-sm"
+          style={{ backgroundColor: btnColor }}
         >
           start
         </button>
@@ -43,7 +45,7 @@
 // Question Display Component
 // ─────────────────────────────────────────────────────────────────────────────
 function QuestionDisplay({ 
-  question, index, total, onNext, onPrev, responses, setResponses 
+  question, index, total, onNext, onPrev, responses, setResponses, buttonColor 
 }: { 
   question: Question, index: number, total: number, onNext: () => void, onPrev: () => void,
   responses: Record<number, any>, setResponses: (r: any) => void
+  buttonColor?: string
 }) {
   const [val, setVal] = useState<any>(responses[question.id] || '');
   const inputRef = React.useRef<HTMLInputElement | HTMLTextAreaElement>(null);
@@ -148,7 +150,7 @@
               setVal(newArr);
             }}
             className={`w-full flex items-center justify-between p-4 rounded-lg border-2 transition-colors
-              ${val.includes(opt) ? 'border-blue-600 bg-blue-50' : 'border-gray-200 hover:border-blue-300'}`}
+              ${val.includes(opt) ? 'border-blue-600 bg-blue-50' : 'border-gray-200'}`}
           >
             <span className="font-medium text-gray-800">{opt}</span>
@@ -194,7 +196,8 @@
           <button
             onClick={handleNext}
             className="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-6 rounded transition-colors text-lg flex items-center gap-2"
+            style={{ backgroundColor: buttonColor || '#2563eb' }}
           >
             OK <ChevronDown size={20} />
           </button>
@@ -390,7 +393,12 @@
     exitUp: { y: 60, opacity: 0 },
   };
 
+  const bgColor = form?.design_settings?.bgColor || '#ffffff';
+  const textColor = form?.design_settings?.textColor || '#111827';
+  const fontFamily = form?.design_settings?.fontFamily === 'mono' ? 'monospace' : form?.design_settings?.fontFamily === 'serif' ? 'serif' : 'sans-serif';
+  const btnColor = form?.design_settings?.buttonColor || '#2563eb';
+
   return (
-    <div className="flex flex-col h-screen bg-white overflow-hidden">
+    <div className="flex flex-col h-screen overflow-hidden" style={{ backgroundColor: bgColor, color: textColor, fontFamily }}>
       {/* Top blue progress bar */}
       <div className="h-1 bg-gray-200 w-full flex-shrink-0">
@@ -398,7 +406,8 @@
           className="h-full bg-blue-600 transition-all duration-500 ease-out"
           style={{ width: `${progress}%` }}
+          style={{ width: `${progress}%`, backgroundColor: btnColor }}
         />
       </div>
 
       {/* Content */}
       {phase === 'welcome' ? (
@@ -410,6 +419,7 @@
             onPrev={() => setCurrentQIndex(prev => Math.max(0, prev - 1))}
             responses={responses}
             setResponses={setResponses}
+            buttonColor={btnColor}
           />
         </motion.div>
