--- frontend/src/app/builder/[id]/page.tsx
+++ frontend/src/app/builder/[id]/page.tsx
@@ -873,6 +873,7 @@
 
             <aside className="w-[272px] bg-[#f9f9f9] border-r border-gray-200 flex flex-col flex-shrink-0">
-              {/* Removed Universal mode selector */}
-
+              {activeTab === 'design' ? (
+                <div className="flex-1 overflow-y-auto p-5">
+                  <h3 className="text-sm font-semibold text-gray-900 mb-4">Design</h3>
+                  <div className="space-y-4">
+                    <div>
+                      <label className="block text-xs font-medium text-gray-700 mb-1">Background Color</label>
+                      <div className="flex items-center gap-2">
+                        <input type="color" value={designSettings.bgColor} onChange={e => handleDesignChange('bgColor', e.target.value)} className="w-8 h-8 rounded cursor-pointer border-0 p-0" />
+                        <span className="text-sm text-gray-500 uppercase">{designSettings.bgColor}</span>
+                      </div>
+                    </div>
+                    <div>
+                      <label className="block text-xs font-medium text-gray-700 mb-1">Text Color</label>
+                      <div className="flex items-center gap-2">
+                        <input type="color" value={designSettings.textColor} onChange={e => handleDesignChange('textColor', e.target.value)} className="w-8 h-8 rounded cursor-pointer border-0 p-0" />
+                        <span className="text-sm text-gray-500 uppercase">{designSettings.textColor}</span>
+                      </div>
+                    </div>
+                    <div>
+                      <label className="block text-xs font-medium text-gray-700 mb-1">Button Color</label>
+                      <div className="flex items-center gap-2">
+                        <input type="color" value={designSettings.buttonColor} onChange={e => handleDesignChange('buttonColor', e.target.value)} className="w-8 h-8 rounded cursor-pointer border-0 p-0" />
+                        <span className="text-sm text-gray-500 uppercase">{designSettings.buttonColor}</span>
+                      </div>
+                    </div>
+                    <div>
+                      <label className="block text-xs font-medium text-gray-700 mb-1">Font Family</label>
+                      <select value={designSettings.fontFamily} onChange={e => handleDesignChange('fontFamily', e.target.value)} className="w-full text-sm border-gray-300 rounded-md shadow-sm p-2 outline-none">
+                        <option value="sans">Sans-serif</option>
+                        <option value="serif">Serif</option>
+                        <option value="mono">Monospace</option>
+                      </select>
+                    </div>
+                  </div>
+                </div>
+              ) : (
-              <div className="flex-1 overflow-y-auto p-3">
+              <div className="flex-1 overflow-y-auto p-3 flex flex-col h-full">
                 <h3 className="text-[11px] font-semibold text-gray-400 uppercase tracking-wider mb-2 px-1">Pages</h3>
@@ -904,6 +904,7 @@
                   </button>
                 </div>
               </div>
+              )}
             </aside>
